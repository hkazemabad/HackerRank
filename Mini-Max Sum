void miniMaxSum(vector<int> arr) {
    
    sort(arr.begin(), arr.end());
    
    int64_t min = 0, max = 0;
    for (size_t i = 0, j = 1; i < 4 && j < 5; ++i, ++j)
    {     
        min += arr[i];
        max += arr[j];
    }
    
    cout << min << " " << max;
    

}
